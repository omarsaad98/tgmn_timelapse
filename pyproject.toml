[project]
name = "tgmn-timelapse"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pydantic-settings>=2.12.0",
    "tzdata>=2025.2",  # Timezone database for Windows (zoneinfo fallback)
]
